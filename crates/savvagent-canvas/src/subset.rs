//! Subset validator stub. Real implementation lands in Task 12.

/// Validate `_source` and emit `tracing::warn!` for anything outside
/// the documented subset. Returns the count of warnings emitted.
pub fn validate(_source: &str) -> usize {
    0
}
